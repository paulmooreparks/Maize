diff --git a/Makefile b/Makefile
index 1a0074f..5ae3a54 100644
--- a/Makefile
+++ b/Makefile
@@ -7,11 +7,13 @@ SRC      = main.c util.c parse.c cfg.c mem.c ssa.c alias.c load.c copy.c \
            fold.c live.c spill.c rega.c gas.c
 AMD64SRC = amd64/targ.c amd64/sysv.c amd64/isel.c amd64/emit.c
 ARM64SRC = arm64/targ.c arm64/abi.c arm64/isel.c arm64/emit.c
-SRCALL   = $(SRC) $(AMD64SRC) $(ARM64SRC)
+MAIZESRC = maize/targ.c maize/abi.c maize/isel.c maize/emit.c maize/data.c
+SRCALL   = $(SRC) $(AMD64SRC) $(ARM64SRC) $(MAIZESRC)
 
 AMD64OBJ = $(AMD64SRC:%.c=$(OBJDIR)/%.o)
 ARM64OBJ = $(ARM64SRC:%.c=$(OBJDIR)/%.o)
-OBJ      = $(SRC:%.c=$(OBJDIR)/%.o) $(AMD64OBJ) $(ARM64OBJ)
+MAIZEOBJ = $(MAIZESRC:%.c=$(OBJDIR)/%.o)
+OBJ      = $(SRC:%.c=$(OBJDIR)/%.o) $(AMD64OBJ) $(ARM64OBJ) $(MAIZEOBJ)
 
 CFLAGS += -Wall -Wextra -std=c99 -g -pedantic
 
@@ -27,11 +29,13 @@ $(OBJDIR)/timestamp:
 	@mkdir -p $(OBJDIR)
 	@mkdir -p $(OBJDIR)/amd64
 	@mkdir -p $(OBJDIR)/arm64
+	@mkdir -p $(OBJDIR)/maize
 	@touch $@
 
 $(OBJ): all.h ops.h
 $(AMD64OBJ): amd64/all.h
 $(ARM64OBJ): arm64/all.h
+$(MAIZEOBJ): maize/all.h
 $(OBJDIR)/main.o: config.h
 
 config.h:
diff --git a/all.h b/all.h
index f49b4ef..31769dc 100644
--- a/all.h
+++ b/all.h
@@ -53,6 +53,7 @@ struct Target {
 	void (*abi)(Fn *);
 	void (*isel)(Fn *);
 	void (*emitfn)(Fn *, FILE *);
+	void (*emitdat)(Dat *, FILE *); /* maize target: mazm data emission */
 };
 
 #define BIT(n) ((bits)1 << (n))
@@ -145,7 +146,7 @@ enum J {
 	X(jfisle) X(jfislt) X(jfiuge) X(jfiugt) \
 	X(jfiule) X(jfiult) X(jffeq)  X(jffge)  \
 	X(jffgt)  X(jffle)  X(jfflt)  X(jffne)  \
-	X(jffo)   X(jffuo)
+	X(jffo)   X(jffuo)  X(hlt)
 #define X(j) J##j,
 	JMPS(X)
 #undef X
diff --git a/cfg.c b/cfg.c
index 81da842..7220466 100644
--- a/cfg.c
+++ b/cfg.c
@@ -323,7 +323,7 @@ simpljmp(Fn *fn)
 		if (b->s2)
 			uffind(&b->s2, uf);
 		c = b->jmp.type - Jjf;
-		if (0 <= c && c <= NCmp)
+		if (0 <= c && c <= NCmp && b->jmp.type != Jhlt)
 		if (b->s1 == b->s2) {
 			b->jmp.type = Jjmp;
 			b->s2 = 0;
diff --git a/fold.c b/fold.c
index 50a862e..13d14f2 100644
--- a/fold.c
+++ b/fold.c
@@ -148,7 +148,7 @@ visitjmp(Blk *b, int n, Fn *fn)
 		flowrk = &edge[n][0];
 		break;
 	default:
-		if (isret(b->jmp.type))
+		if (isret(b->jmp.type) || b->jmp.type == Jhlt)
 			break;
 		die("unreachable");
 	}
diff --git a/main.c b/main.c
index abef591..ea5e477 100644
--- a/main.c
+++ b/main.c
@@ -7,6 +7,7 @@ Target T;
 
 extern Target T_amd64_sysv;
 extern Target T_arm64;
+extern Target T_maize;
 
 static struct TMap {
 	char *name;
@@ -14,6 +15,7 @@ static struct TMap {
 } tmap[] = {
 	{ "amd64_sysv", &T_amd64_sysv },
 	{ "arm64", &T_arm64 },
+	{ "maize", &T_maize },
 	{ 0, 0 }
 };
 
@@ -44,10 +46,14 @@ data(Dat *d)
 	if (dbg)
 		return;
 	if (d->type == DEnd) {
-		fputs("/* end data */\n\n", outf);
+		if (!T.emitdat)
+			fputs("/* end data */\n\n", outf);
 		freeall();
 	}
-	gasemitdat(d, outf);
+	if (T.emitdat)
+		T.emitdat(d, outf);
+	else
+		gasemitdat(d, outf);
 }
 
 static void
@@ -99,7 +105,8 @@ func(Fn *fn)
 			fn->rpo[n]->link = fn->rpo[n+1];
 	if (!dbg) {
 		T.emitfn(fn, outf);
-		fprintf(outf, "/* end function %s */\n\n", fn->name);
+		if (!T.emitdat)
+			fprintf(outf, "/* end function %s */\n\n", fn->name);
 	} else
 		fprintf(stderr, "\n");
 	freeall();
@@ -193,7 +200,7 @@ main(int ac, char *av[])
 		parse(inf, f, data, func);
 	} while (++optind < ac);
 
-	if (!dbg) {
+	if (!dbg && !T.emitdat) {
 		gasemitfin(outf);
 		if (asm == Gaself)
 			fprintf(outf, ".section .note.GNU-stack,\"\",@progbits\n");
diff --git a/parse.c b/parse.c
index 104ca1d..e431116 100644
--- a/parse.c
+++ b/parse.c
@@ -37,6 +37,7 @@ enum {
 	Tjmp,
 	Tjnz,
 	Tret,
+	Thlt,
 	Texport,
 	Tfunc,
 	Ttype,
@@ -87,6 +88,7 @@ static char *kwmap[Ntok] = {
 	[Tjmp] = "jmp",
 	[Tjnz] = "jnz",
 	[Tret] = "ret",
+	[Thlt] = "hlt",
 	[Texport] = "export",
 	[Tfunc] = "function",
 	[Ttype] = "type",
@@ -600,6 +602,9 @@ parseline(PState ps)
 			curb->jmp.arg = r;
 		}
 		goto Close;
+	case Thlt:
+		curb->jmp.type = Jhlt;
+		goto Close;
 	case Tjmp:
 		curb->jmp.type = Jjmp;
 		goto Jump;
@@ -1277,6 +1282,9 @@ printfn(Fn *fn, FILE *f)
 			if (b->s1 != b->link)
 				fprintf(f, "\tjmp @%s\n", b->s1->name);
 			break;
+		case Jhlt:
+			fprintf(f, "\thlt\n");
+			break;
 		default:
 			fprintf(f, "\t%s ", jtoa[b->jmp.type]);
 			if (b->jmp.type == Jjnz) {
